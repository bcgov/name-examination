/**
 * Enum for Entity type codes.
 * Used for mapping conversions to support namex ui's split entityTypes.
 * */
const EntityTypeCode = {
  GP: 'GP', // General Partnership
}

export default Object.freeze(EntityTypeCode)
